<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grishman.rssfeed">

    <!--Permissions-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:parentActivityName=".MainActivity">
            <!--android:theme="@android:style/Theme.NoTitleBar">-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.grishman.rssfeed.MainActivity" />
        </activity>

        <provider
            android:name="com.grishman.rssfeed.data.RSSFeedProvider"
            android:authorities="@string/content_authority" />
        <service android:name=".service.FeedParserService" />
        <receiver
            android:name=".service.FeedParserService$AlarmReceiver"
            android:enabled="true" />
    </application>

</manifest>
